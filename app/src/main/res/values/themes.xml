<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GroWise" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="CustomDatePickerTheme" parent="Theme.Material3.DayNight.Dialog">
        <!-- Background color -->
        <item name="android:background">#000000</item>

        <!-- CalendarView/Spinner colors -->
        <item name="android:datePickerStyle">@style/CustomDatePickerStyle</item>
    </style>

    <style name="CustomDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <item name="android:textColorPrimary">#ffffff</item>
        <item name="android:headerBackground">#ffffff</item>
        <item name="android:dayOfWeekTextAppearance">@style/CustomDatePickerDayText</item>
    </style>

    <style name="CustomDatePickerDayText" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:fontFamily">@font/classy_vogue</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>